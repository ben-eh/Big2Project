/*
    This file will be the things that interacts with your json database locally.
    This will be using the file-helper.
    These are technically your tables in your database.
*/

import { GameDB, PlayerDB } from "./types"

export default class StatsDatabase {
    public createGame = (game: GameDB) => {
        // Read json file
        // Update json file
        // Write to json file new database contents
    }

    public createPlayer = (player: PlayerDB) => {
        // Read json file
        // Update json file
        // Write to json file new database contents
    }

    public updatePlayer = () => {
        // Read json file
        // Update json file
        // Write to json file new database contents
    }
}
